import dataHelpers from '../../../utils/data-helpers';
import { module, test } from 'qunit';

module('dataHelpers');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dataHelpers();
  assert.ok(result);
});
